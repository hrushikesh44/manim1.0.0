###  simple setup
## Install the dependencies   
`python3 -m venv venv`                                                                  
`source venv/bin/activate`                                                             
`pip install transformers torch peft`                                                   
`python run_cli.py`
